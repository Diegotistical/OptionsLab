# Core data & ML libs
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
scikit-learn>=1.2.0
xgboost>=1.7.0
torch>=2.0.0
numba>=0.60.0
llvmlite>=0.43.0

# cupy-cuda12x   # optional, only if you have a CUDA GPU on the host

# Model explainability and feature processing
joblib>=1.2.0

# Testing & code quality
pytest>=7.2.0
pytest-cov>=4.0.0
ruff>=0.1.0
black>=23.0.0
isort>=5.12.0

# Graphs
matplotlib>=3.8.0
plotly>=5.20.0

# Optional but useful for CI and debugging
flake8>=6.0.0

# Streamlit for your UI (if you want to add it later)
streamlit>=1.32.0

# Optimization & Export (pinned for ONNX stability)
optuna>=3.5.0,<4.0.0
onnx>=1.15.0,<1.17.0
onnxruntime>=1.17.0,<1.19.0
onnxmltools>=1.12.0,<1.13.0
skl2onnx>=1.16.0,<1.18.0
protobuf>=4.25.0,<5.0.0

# Pin for scikit-learn 1.6+ compatibility (force_all_finite -> ensure_all_finite fix)
lightgbm>=4.6.0
pyarrow>=14.0.0

# Market data pipeline
yfinance>=0.2.0

